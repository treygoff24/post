#!/bin/sh
# Thin descriptor: launch grok through the identity helper as harness
# 'grok'. Adding a harness is one file like this one — nothing else.
dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec "$dir/../agent-session" --harness grok -- grok "$@"
