#!/bin/sh
# Thin descriptor: launch cursor-agent through the identity helper as harness
# 'cursor'. Adding a harness is one file like this one — nothing else.
dir=$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)
exec "$dir/../agent-session" --harness cursor -- cursor-agent "$@"
