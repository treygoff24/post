[package]
name = "post"
version = "0.2.0"
edition = "2021"
description = "Machine-local mailbox for AI agents"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thiserror = "2"
libc = "0.2"
